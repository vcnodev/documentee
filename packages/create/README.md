# create-documentee

Starter wrapper for creating a new Documentee docs project.

```bash
pnpm create documentee my-docs
```

The generated project can be validated, built, served in dev mode, or previewed as static output with `@documentee/cli`.

See the [root README](../../README.md) and [CLI README](../cli/README.md).
