# @documentee/openapi

OpenAPI loading and compact operation normalization for Documentee.

The package keeps operation metadata small by extracting route, auth, parameters, request body, responses, and schema-reference names without inlining large schema graphs into every endpoint page.
